mixin header(title, color)
  .header
    .pull-left
      if block
        block
    h1.pull-left(class!=attributes.class) #{title}
    .email.pull-right
    .clearfix

mixin icon-and-text(title, contents)
  .col-md-4
    div(class!=attributes.class)
    h2 #{title}
    .text2 #{contents}


